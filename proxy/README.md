# Proxy Options

## `arbitrage.py` (Recommended)
Python proxy with free model routing + ledger tracking — see repo root for usage.

## Legacy JS Proxy
An earlier Node.js version existed here but was removed to avoid hardcoded API keys.
Use `arbitrage.py` instead.
